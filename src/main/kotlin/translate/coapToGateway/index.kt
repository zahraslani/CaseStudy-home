package translate.coapToGateway

fun coolerToCooler(cooler: String): String {
    // define how target is computed from source
    return cooler
} 

fun heaterToHeater(heater: String): String {
    // define how target is computed from source
    return heater
} 

fun lightToLight(light: String): String {
    // define how target is computed from source
    return light
} 

fun windowToWindow(window: String): String {
    // define how target is computed from source
    return window
} 

fun alarmToAlarm(alarm: String): String {
    // define how target is computed from source
    return alarm
} 

fun extinguisherToExtinguisher(extinguisher: String): String {
    // define how target is computed from source
    return extinguisher
} 
