package translate.gatewayToMqtt

fun lightToCurtain(light: String): String {
    // define how target is computed from source
    return light
} 

fun lightToLamp(light: String): String {
    // define how target is computed from source
    return light
} 
