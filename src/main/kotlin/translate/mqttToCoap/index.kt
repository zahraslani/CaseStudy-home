package translate.mqttToCoap

fun temperatureToCooler(temperature: String): String {
    // define how target is computed from source
    return temperature
} 

fun temperatureToHeater(temperature: String): String {
    // define how target is computed from source
    return temperature
} 

fun smokeToWindow(smoke: String): String {
    // define how target is computed from source
    return smoke
} 

fun smokeToAlarm(smoke: String): String {
    // define how target is computed from source
    return smoke
} 

fun smokeToExtinguisher(smoke: String): String {
    // define how target is computed from source
    return smoke
} 
